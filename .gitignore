//We remove the tests files, because they may contain javascript files which may contain sensitive informations
Tests/*
//!Tests/TestBaseAPIs.sql
!Tests/*.sql

//Ignore tests...
testNodeJS.js
ignoreFiles.json


//We ignore the config.php file, because the config depends on the system
config.php

//We don't provide the documentation
API/Documentation.txt

*.DS_Store